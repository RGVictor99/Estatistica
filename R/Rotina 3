#Criando  gr�ficos de barra ou coluna para dados qualitativos###

#Criando  gr�ficos das FIGURA 3 e 4   ###
 
 
dados_quali<-matrix(0,1,2)

colnames(dados_quali)<-c("Branca","Vermelha")
dados_quali [1,1]<-25
dados_quali [1,2]<-75

barplot(dados_quali [1,],col=c("blue","red"),horiz=F,beside=F,space=0.3,
xlab="Tipo de Carne", ylab="N�mero de crian�as") 

text(locator(n=2),c("25%","75%")) 

#tem que dar um clique, com o mouse, indicando o local que vai aparcer o valor na ordem.


# Criando  gr�ficos de Setores (Pizza) ###

pie(dados_quali [1,],col=c("blue","red"))
text(locator(n=2),c("25%","75%"))
#tem que dar um clique, com o mouse, indicando o local que vai aparcer o valor na ordem.



